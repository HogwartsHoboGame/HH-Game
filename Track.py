class Track:
        def __init__(self, track_num):
                self.track_num = track_num
                self.has_train = False
        
        def train_pass():
                self.has_train = True
                
